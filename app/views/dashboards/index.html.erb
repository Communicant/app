<p id="notice"><%= notice %></p>

<h1>Dashboard</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Email</th>
      <th>Phone number</th>
      <th>Child name</th>
      <th>Case number</th>
      <th>Mediator name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @dashboards.each do |profile| %>
      <tr>
        <td><%= dashboard.name %></td>
        <td><%= dashboard.email %></td>
        <td><%= dashboard.phone_number %></td>
        <td><%= dashboard.child_name %></td>
        <td><%= dashboard.case_number %></td>
        <td><%= dashboard.mediator_name %></td>
        <td><%= link_to 'Show', dashboard %></td>
        <td><%= link_to 'Edit', edit_dashboard_path(dashboard) %></td>
        <td><%= link_to 'Destroy', dashboard, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Profile', new_profile_path %>
